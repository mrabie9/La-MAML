# Model-specific overrides for packnet.
model: packnet
loader: task_incremental_loader
lr: 0.03
prune_perc: 0.75
# Optional SGD epochs on this task's train data after packing; grads only on
# weights newly assigned to the task (owner == task). 0 disables finetune.
post_prune_epochs: 0

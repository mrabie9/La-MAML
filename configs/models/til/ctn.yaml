# Model-specific overrides for ctn.
model: ctn
loader: task_incremental_loader

# Total inner base SGD steps per observe (replaces legacy inner_steps × n_meta; was 2×2).
inner_steps: 4

lr: 0.03
ctx_lr: 0.003
task_emb: 64
memory_strength: 10

validation: 0.3